{{ if .Values.pull_secret.enabled }}
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: {{ .Values.pull_secret.name }}
spec:
  path: {{ .Values.pull_secret.path }}
  type: kubernetes.io/dockerconfigjson
{{ end }}
