apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ template "helpers.fullname" . }}-pull-networkpolicy
spec:
  podSelector:
    matchLabels:
      cachemachine: pull
  policyTypes:
    - Ingress
    - Egress
