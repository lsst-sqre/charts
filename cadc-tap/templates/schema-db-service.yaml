kind: Service
apiVersion: v1
metadata:
  name: {{ template "cadc-tap.fullname" . }}-schema-db
  labels:
    app: {{ template "cadc-tap.fullname" . }}-schema-db
    chart: {{ template "cadc-tap.chart" . }}
spec:
  selector:
    app: {{ template "cadc-tap.fullname" . }}-schema-db
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
